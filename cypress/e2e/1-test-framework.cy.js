describe('login feature', () => {

   before(() => {
      cy.log('Before all tests')
   });
   beforeEach(() => {
      cy.log('Before each test')
   });
   it('login pass', () => {
      cy.log('')
   });

   it('login failed wiith invalid user', () => {
      
   });

   it('login failed with invalid password', () => {
      
   });
});